package com.example.fjnuserviceapp.base;

import androidx.lifecycle.ViewModel;

/**
 * 所有ViewModel的基类，封装通用逻辑
 */
public class BaseViewModel extends ViewModel {
    // 可以在这里添加通用的ViewModel逻辑，如错误处理、加载状态管理等
}